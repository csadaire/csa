<%= form_with(model: poetry, local: true) do |form| %>
  <% if poetry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(poetry.errors.count, "error") %> prohibited this poetry from being saved:</h2>

      <ul>
      <% poetry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :poetry_title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :poetry_body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
